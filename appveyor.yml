version: 1.1.{build}-{branch}

environment:
  matrix:
    - platform: x86

    - platform: x64

init:
  - "SET PATH = %PATH%;C:\\MinGW\\bin"
  - "SET"
  - "C:\\MinGW\\bin\\gcc.exe -v"
  - "gcc.exe -v"

install:
  - "cd %APPVEYOR_BUILD_FOLDER%"
  - "gradlew installTestsDebugExecutable"
  - "gradlew installTestVectorsGeneratorDebug"

test_script:
  - "cd  %APPVEYOR_BUILD_FOLDER%/build/install/tests/debug/"
  - "tests.bat"
