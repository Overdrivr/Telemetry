apply plugin: 'cpp'

model {
    buildTypes {
        debug
        release
    }

    toolChains {
      gcc(Gcc) {

      }
    }


  components {
    telemetry(NativeLibrarySpec) {
      
    }
    main(NativeExecutableSpec) {
        binaries.all {
          lib library: "telemetry"
        }
	  }
    tests(NativeExecutableSpec) {
       binaries.all {
         lib library: "telemetry"
       }
     }
   }
}
