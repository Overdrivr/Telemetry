apply plugin: 'c'

model {
    buildTypes {
        debug
        release
    }

    toolChains {
      gcc(Gcc) {

      }
    }


  components {
    telemetry(NativeLibrarySpec) {

    }
    crc(NativeLibrarySpec){

    }
    main(NativeExecutableSpec) {
        binaries.all {
          lib library: "telemetry"
          lib library: "crc"
        }
	  }
    tests(NativeExecutableSpec) {
       binaries.all {
         lib library: "telemetry"
         lib library: "crc"
       }
     }
   }
}
