apply plugin: 'c'

model {
    buildTypes {
        debug
        release
    }

    toolChains {
      gcc(Gcc) {

      }
    }


    components {
     telemetry(NativeLibrarySpec) {

    	}
      main(NativeExecutableSpec) {
        binaries.all {
          lib library: "telemetry"
        }
	   }
   }
}
